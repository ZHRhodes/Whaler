mutation saveContacts($senderID: ID, $input: [NewContact!]!) {
  saveContacts(senderID: $senderID, input: $input) {
    id
    firstName
    lastName
    jobTitle
    email
    phone
    salesforceID
    accountID
    state
    assignedTo
  }
}
