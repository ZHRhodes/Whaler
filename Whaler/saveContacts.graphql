mutation saveContacts($input: [NewContact!]!) {
  saveContacts(input: $input) {
    id
    firstName
    lastName
    jobTitle
    email
    phone
    salesforceID
    accountID
    state
    assignedTo
  }
}
